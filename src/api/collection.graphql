query CollectionGetList {
	collections {
		id
		name
		slug
	}
}

query CollectionGetById($id: ID!) {
	collection(where: { id: $id }) {
		id
		name
		slug
		description
		products {
			...ProductListItem
		}
	}
}
